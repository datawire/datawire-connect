Datawire Connect relies on Node.js and its package manager, NPM.

The easiest way to get NPM is to install Node.js, which you need to do
anyway to run the Javascript server examples! Check out

https://docs.npmjs.com/getting-started/installing-node

for more information about Node.

