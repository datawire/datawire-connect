Datawire Connect relies on a service called "hello", which must be created
using the dwc command. The simplest way to handle this if you haven't already
created a "hello" token is to run

make token

but you can do it by hand if you like:

dwc create-service hello

is all you need to run.

